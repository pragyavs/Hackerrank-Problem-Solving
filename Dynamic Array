function rotateLeft(d, arr) {
    let temp;
    console.log(arr)
    for (let i = 1; i <= d; i++) {
        for (let i = 0; i < arr.length - 1; i++) {
            temp = arr[i];
            arr[i] = arr[i + 1]
            arr[i + 1] = temp;
        }
    }
    return arr;
}